import { render } from "@testing-library/react";
import { Index } from "../Index";

describe("Index route", () => {
  beforeEach(() => {
    render(<Index></Index>);
  });
  test("Should render", () => {});
});
